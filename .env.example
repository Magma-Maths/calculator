DOMAIN=calc.magma-maths.org

# Image tag to run from ghcr.io/magma-maths/calculator: sha-<7 hex chars> or vX.Y.Z.
# Empty follows the moving "main" tag, which is republished on every push to main.
CALCULATOR_VERSION=
